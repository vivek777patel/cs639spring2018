<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/vertical_margin">

    <LinearLayout
        android:id="@+id/imgTxtViewLL"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_toStartOf="@+id/nextDelBtnLL"
        android:layout_alignParentLeft="true"
        >
        <ImageView
            android:id="@+id/animalImg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/dog"
            android:focusableInTouchMode="false"
            android:focusable="false"
            />

        <TextView
            android:id="@+id/animalTxt"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="@dimen/text_margin"
            android:layout_marginVertical="20dp"
            android:text="this is an animal"
            android:visibility="invisible" />

    </LinearLayout>

    <LinearLayout
        android:id="@+id/nextDelBtnLL"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_alignParentRight="true"
        android:layout_gravity="right"
        >

            <Button
                android:id="@+id/descNextBtn"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/next_button"
                android:visibility="invisible"
                android:focusableInTouchMode="false"
                android:focusable="false"
            />

            <Button
                android:id="@+id/descDelBtn"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/delete_button"
                android:visibility="invisible"
                android:focusableInTouchMode="false"
                android:focusable="false"
            />

    </LinearLayout>


</RelativeLayout>